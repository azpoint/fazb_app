import { Great_Vibes, Open_Sans } from "next/font/google";

export const dancingScript = Great_Vibes({
	weight:["400"],
	subsets: ["latin"],
	variable: "--font-dancingScript",
});

export const openSans = Open_Sans({
	subsets: ["latin"],
	variable: "--font-openSans",
});